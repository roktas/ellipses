#!/usr/bin/env ruby
# frozen_string_literal: true

require 'ellipses/client/cli'

def main
  Dry::CLI.new(Ellipses::Client::CLI::Commands).()
rescue Ellipses::Error => e
  abort Ellipses::Support.error e.message
end

main
